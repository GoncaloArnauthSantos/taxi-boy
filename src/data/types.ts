export type Tour = {
    id: string
    creationDate: string
    updateDate: string
    title: string
    description: string
    images: string[]
    bannerImage: string
    duration: number
    price: number
    locations: string[]
}
